@model IEnumerable<DishClassLibrary.temp.test.Get_Ingredients>

@{
    ViewBag.Title = "GetIngredients";
}

<h2>GetIngredients</h2>

<p>
    @Html.ActionLink("Create New", "SetIngredient")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ingredient)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Info)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ingredient)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Info)
            </td>
            <td></td>
        </tr>
    }

</table>
@Html.ActionLink("Back", "Mainpage")
